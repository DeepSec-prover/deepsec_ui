/**
 * Demonstration code for the code style setting
 *
 * @type {string} Some spec code
 */
const demoCode = `// Example code
new skT;
let pkT_42 = pk(skT) in
(
   !~6
   out(ch,adec(x,skT))
) | (
   out(ch,adec(x_2,skT))
   0
)
`

export default demoCode
